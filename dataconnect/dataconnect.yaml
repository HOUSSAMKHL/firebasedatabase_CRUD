specVersion: "v1beta"
serviceId: "firebasedatabase_CRUD"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebasedatabase_CRUD-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
